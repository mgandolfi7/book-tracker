<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.3/css/all.min.css">
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
    <h1>Current Books</h1>

    <ul class="books">
        <% for (let i = 0; i < info.length; i++) {%>
            <li class="book">
                <span><%= info[i].bookTitle %></span>
                <span><%= info[i].bookAuthor %></span>
                <span><%= info[i].checkoutDate %></span>
                <span class="fa fa-trash"></span>
            </li>
        <% } %>
    </ul>

    <h2>Add a Book:</h2>

    <form action="/addBook" method="POST">
        <input type="text" placeholder="Book Title" name="bookTitle">
        <input type="text" placeholder="Book Author" name="bookAuthor">
        <input type="date" placeholder="Checkout Date" name="checkoutDate">
        <input type="submit">
    </form>

    <script src="js/main.js"></script>

</body>
</html>